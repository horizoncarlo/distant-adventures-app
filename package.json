{
  "name": "distant-adventures-app",
  "version": "0.9",
  "scripts": {
    "build": "bun build backend/server.js --outdir ./build",
    "watch": "bun --watch backend/server.js",
    "hot": "bun --hot backend/server.js"
  },
  "private": true
}
